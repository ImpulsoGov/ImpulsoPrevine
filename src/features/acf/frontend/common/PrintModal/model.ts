export type ModalLabels = {
    title: string;
    primaryCustomOption: {
        title: string;
        description: string;
        splitTeam: string;
        noSplit: string;
    };
    secondaryCustomOption: {
        title: string;
        recomendation: string;
        splitGroupPerPage: string;
        order?: string;
    };
    button: string;
};
