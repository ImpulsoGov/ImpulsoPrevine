import type React from "react";
import { UI } from "./ui";

export const SearchPlusPage: React.FC = () => {
    return (
        <>
            <UI></UI>
            <div id="ig-csvpdf"></div>
        </>
    );
};
